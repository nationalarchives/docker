#!/bin/bash

if [ -z $1 ]
then
  echo "Usage: $0 wsgi_application";
  echo -e "\twsgi_application is the WSGI application from Django you want to run, e.g. app.wsgi:application"
  exit 1
fi

tna-run $1

poetry run python manage.py migrate
