#!/bin/bash

cd /app || return

if [ -z "$1" ] || [ "$1" == "poetry" ]; then
  echo "Upgrading Poetry dependencies..."
  poetry update
fi

if [ -z "$1" ] || [ "$1" == "npm" ]; then
  echo "Upgrading npm dependencies..."
  tna-npm update
fi
