version: '3'

services:
  app:
    container_name: my-tna-application
    build: .
    environment:
      - ENVIRONMENT=develop
      - THREADS=5
      - LOG_LEVEL=INFO
    ports:
      - 65535:8080
    volumes:
      - .:/app
